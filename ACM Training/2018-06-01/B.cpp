#include <cstdio>
using namespace  std;
int T;
int f[15][15];
int main()
{
    f[1][1] = 1;
    for (int i = 1; i <= 10; ++i)
        for (int j = i + 1; j <= 10; ++j)
            f[i][j] = f[i - 1][j - 1] + f[i - 1][j] 
    
    scanf("%d", &T);
    while (T--)

}